# Add directories or file patterns to ignore during indexing (e.g. foo/ or *.csv)
# /data/
/infra/
/types/
/tmp/
/examples/
/coverage/
# Ignore all directories that start with `.` at root
/.*
!/.circleci
!.env
.next
dist
