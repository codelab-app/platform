import { TagNode } from './antd-tag-tree.data'

export const createTagSeedData = () => {
  const parseTagNode = (node: TagNode) => {
    //
  }

  // for (const [parentTag, tagNodes] of ObjectTyped.entries(antdTagTree)) {
  //   tagNodes.map((tagNode) => {
  //     return parseTagNode(tagNode)
  //   })
  // }
}
