{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "id": "ApiUseCase",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "E.g. \"app\", this is the name of the api model",
      "enum": [
        "admin",
        "app",
        "atom",
        "element",
        "hook",
        "lambda",
        "page",
        "tag",
        "type",
        "user"
      ],
      "default": ""
    },
    "useCase": {
      "description": "E.g. \"create\", use lower camel case. The model will be prepended automatically",
      "x-prompt": "What name would you like to use for the use case?"
    }
  },
  "required": ["name", "useCase"]
}
