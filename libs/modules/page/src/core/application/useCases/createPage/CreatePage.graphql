mutation CreatePage($input: CreatePageInput!) {
  createPage(input: $input) {
    # ...pageFragments
    id
    title
    graphs {
      id
      label
      vertices {
        id
        type
      }
    }
  }
}
