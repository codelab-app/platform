query GetPageElementOwner($pageElementId: ID!) {
    getPageElement(id: $pageElementId){
        page {
            app {
                ownerId
            }
        }
        ...Dgraph_PageElement
    }
}