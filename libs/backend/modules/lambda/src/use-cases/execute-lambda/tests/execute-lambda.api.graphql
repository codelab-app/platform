# import __LambdaPayload from '../../../domain/lambda-payload.fragment.api.graphql'

mutation ExecuteLambda($input: ExecuteLambdaInput!) {
  executeLambda(input: $input) {
    ...__LambdaPayload
  }
}
