query elementDependentTypes {
  elements {
    dependantTypes {
      ... on EnumType {
        __typename
        id
      }
      ... on ArrayType {
        __typename
        id
      }
      ... on UnionType {
        __typename
        id
      }
    }
  }
}
