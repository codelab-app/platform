query pageResolverPages {
  pages {
    elements {
      id
    }
    id
    name
    rootElement {
      id
    }
    slug
    dependantTypes {
      ... on EnumType {
        __typename
        id
      }
      ... on ArrayType {
        __typename
        id
      }
      ... on UnionType {
        __typename
        id
      }
    }
  }
}
