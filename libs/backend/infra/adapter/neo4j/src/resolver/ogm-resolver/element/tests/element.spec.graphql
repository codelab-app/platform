query elementDependentTypes {
  elements {
    dependantTypes {
      ... on EnumType {
        id
        name
      }
      ... on ArrayType {
        id
        name
      }
      ... on UnionType {
        id
        name
      }
    }
  }
}
