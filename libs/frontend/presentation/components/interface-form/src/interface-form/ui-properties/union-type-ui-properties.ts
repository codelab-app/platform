import { SelectUnionTypeValue } from '../fields/select-union-type-value/SelectUnionTypeValue'

export const unionTypeUiProperties = () => {
  return { uniforms: { component: SelectUnionTypeValue } }
}
