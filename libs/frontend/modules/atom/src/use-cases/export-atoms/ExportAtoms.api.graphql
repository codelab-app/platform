fragment AtomExport on Atom {
  id
  name
  type
  api {
    id
    name
    typeKind
    typeGraph {
      ...__TypeGraph
    }
  }
}
query ExportAtoms($input: GetAtomsInput) {
  getAtoms(input: $input) {
    ...AtomExport
  }
}
