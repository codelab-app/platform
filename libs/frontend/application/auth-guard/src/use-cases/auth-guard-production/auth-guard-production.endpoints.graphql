query GetAuthGuardProduction($domain: String!, $pageUrl: String!) {
  authGuards(
    where: {
      pages_SOME: {
        AND: [
          { app: { domains_SOME: { name: $domain } } }
          { url: $pageUrl }
          { kind: Regular }
        ]
      }
    }
  ) {
    ...AuthGuard
  }
}
