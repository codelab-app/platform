query GetSelectAtomOptions {
  atoms {
    # Need to load API as well
    __typename
    api {
      ...InterfaceType
    }
    id
    name
    requiredParents {
      id
      type
    }
    type
  }
}
