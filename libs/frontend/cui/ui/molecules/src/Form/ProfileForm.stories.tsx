import React from 'react'
import { ProfileForm } from './ProfileForm'

export default {
  component: ProfileForm,
  title: 'Profile Form',
}

export const Default = {
  args: {},
  render: () => <ProfileForm />,
}
