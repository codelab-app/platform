query GetActions($storeId: ID) {
  # codeActions {
  #   ...Action
  # }
  # apiActions {
  #   ...Action
  # }
  codeActions(where: { store: { id: $storeId } }) {
    ...Action
  }
  apiActions(where: { store: { id: $storeId } }) {
    ...Action
  }
}
