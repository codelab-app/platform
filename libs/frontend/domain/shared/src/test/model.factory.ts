import type { IRootDomainStore } from '@codelab/frontend/abstract/domain'

export class ModelFactory {
  constructor(protected rootStore: Partial<IRootDomainStore>) {}

  // protected abstract build(data)
}
