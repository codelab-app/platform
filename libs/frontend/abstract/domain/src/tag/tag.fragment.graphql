fragment Tag on Tag {
  children {
    id
    name
    owner {
      id
    }
  }
  descendants {
    id
    name
  }
  id
  # isRoot
  name
  owner {
    id
  }
  parent {
    id
  }
}

fragment TagPreview on Tag {
  id
  name
  owner {
    id
  }
}
