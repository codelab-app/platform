fragment Tag on Tag {
  children {
    id
    name
  }
  descendants {
    id
    name
  }
  id
  # isRoot
  name
  owner {
    ...Owner
  }
  parent {
    id
  }
}

fragment TagPreview on Tag {
  id
  name
}
