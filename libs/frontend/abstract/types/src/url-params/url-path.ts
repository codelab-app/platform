/**
 * Params are not required until we are in a specific context
 */
export interface UrlPathParams {
  appId?: string
  authGuardId?: string
  componentId?: string
  interfaceId?: string
  pageId?: string
  resourceId?: string
}
