fragment Page on Page {
  id
  name
  slug
  getServerSideProps
  app {
    id
  }
  rootElement {
    ...Element
    descendantElements {
      ...Element
    }
  }
  pageContentContainer {
    id
  }
  kind
}
