fragment Page on Page {
  id
  name
  slug
  getServerSideProps
  app {
    id
  }
  rootElement {
    ...Element
    descendantElements {
      ...Element
    }
  }
  pageContentContainer {
    id
  }
  kind
}

fragment BuilderPage on Page {
  id
  name
  slug
  getServerSideProps
  rootElement {
    ...Element
    descendantElements {
      ...Element
    }
  }
  app {
    id
    owner {
      ...Owner
    }
  }
  pageContentContainer {
    id
  }
  kind
}
