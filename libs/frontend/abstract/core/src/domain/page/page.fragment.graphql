fragment Page on Page {
  id
  name
  slug
  app {
    id
  }
  rootElement {
    ...Element
    descendantElements {
      ...Element
    }
  }
  pageContentContainer {
    id
  }
  kind
}

fragment BuilderPage on Page {
  id
  name
  slug
  rootElement {
    ...Element
    descendantElements {
      ...Element
    }
  }
  app {
    id
    owner {
      ...Owner
    }
  }
  pageContentContainer {
    id
  }
  kind
}
