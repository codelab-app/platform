describe.skip('Menu', () => {
  before(() => {
    // Seed app, 2 pages (Home & About)
  })

  describe('Builder mode', () => {
    it('should not navigate to another page when clicked', () => {
      // Instead assert what should happen in builder mode
    })
  })

  describe('Preview mode', () => {
    it('can navigate to another page using a link', () => {})
  })
})
