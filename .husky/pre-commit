npx validate-branch-name

# Check if there are changes in .circleci directory and user is Webber Wang
if [ "$(git config user.name)" = "Webber Wang" ] && git diff --cached --name-only | grep -q "^\.circleci/"; then
  pnpm cpack
fi

pnpm cli tasks lint --stage=test
