#
# Dockerignore has issues with wildcard exclude
# Copy required files only instead in Dockerfiles
#
# Dockeringore is too slow, https://github.com/moby/moby/issues/23470#issuecomment-233581824
#
# Instead copy all required files to directoy, then go from there

# Ignore everything
*

# # Except
!/package.json
!/pnpm-lock.yaml
!/.npmrc
!/nx.json

!/dist/libs/tools/
!/apps/platform/
