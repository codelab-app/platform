executor: docker-node-amd64
resource_class: large
steps:
  - checkout
  - setup-env
  - install-pnpm
  - install-packages
  - login-docker

  - persist_to_workspace:
      root: ~/
      paths:
        - .docker/config.json
