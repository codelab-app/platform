executor: docker-node
resource_class: large
steps:
  - checkout
  - install-nvm
  - load-nvm
  - install-terraform
  - setup-env
  - login-docker

  - persist_to_workspace:
      root: ~/
      # Persists the docker login
      paths:
        - .docker/config.json
