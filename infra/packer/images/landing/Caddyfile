# Static Caddyfile Configuration
# This is intentionally hardcoded rather than templated for simplicity.
# The domain rarely changes, and rebuilding the image is acceptable
# when it does. This eliminates the consul-template dependency and
# reduces complexity and potential failure points.

codelab.app, www.codelab.app {
    # Matches container_name: codelab-landing from docker-compose.ctmpl
    reverse_proxy codelab-landing:3000
}