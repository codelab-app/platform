---
slug: '/getting-started/getting-started/'
---

# Getting Started

1. `cp .env.example .env` & get env from Slack
2. `yarn`
3. `yarn docker:up hasura`

This will call `hausra` service in `.docker/docker-compose.yaml`, which calls `postgres` as a dependency
